local wezterm = require("wezterm")
local config = require("config")
-- require("events")

-- Apply color scheme
-- Defaulting to "Nord (Gogh)" as the dynamic check depends on an environment variable
config.color_scheme = "Nord (Gogh)"

return config